/** LINKS-only. GROUPED = actionApplicationsCount > 1; UNGROUPED = count === 1. */
export enum ActionPlanOperationalGroupingFilterEnum {
  GROUPED = 'GROUPED',
  UNGROUPED = 'UNGROUPED',
}

export enum ActionPlanOperationalScopeEnum {
  GLOBAL = 'GLOBAL',
  COMPANY = 'COMPANY',
}
