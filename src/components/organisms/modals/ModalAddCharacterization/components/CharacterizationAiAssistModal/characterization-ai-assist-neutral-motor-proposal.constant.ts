/**
 * WIP slot for the neutral motor proposal provided by Alex.
 *
 * Intentionally empty until the full text is pasted into this constant.
 * Never auto-persisted. Loaded only via explicit MASTER action in the
 * Arquitetura do Assistente dialog.
 */
export const CHARACTERIZATION_AI_ASSIST_NEUTRAL_MOTOR_PROPOSAL = '';

export const CHARACTERIZATION_AI_ASSIST_NEUTRAL_MOTOR_PROPOSAL_AVAILABLE =
  CHARACTERIZATION_AI_ASSIST_NEUTRAL_MOTOR_PROPOSAL.trim().length > 0;
