import { css } from '@emotion/react';

import defaultTheme from '../../configs/theme';

const globalStylesDashboard = css`
  body {
    background-color: ${defaultTheme.palette.background.dark};
  }
`;

export default globalStylesDashboard;
