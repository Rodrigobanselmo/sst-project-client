import { Box, styled } from '@mui/material';

export const STFlexContainer = styled(Box)`
  display: flex;
  flex: 1;
  flex-direction: column;
  height: 100%;
  min-height: 0;
  width: 100%;
  position: relative;
  overflow: hidden;
`;
